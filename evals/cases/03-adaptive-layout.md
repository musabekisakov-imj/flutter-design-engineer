# Adaptive tablet composition

Use `$flutter-implementation` to adapt the supplied phone-first schedule screen for compact, medium, and expanded constraints without stretching the phone column.

Acceptance: uses constraints rather than device names; introduces meaningful expanded composition; preserves selection/focus; tests breakpoint boundaries and text scaling.


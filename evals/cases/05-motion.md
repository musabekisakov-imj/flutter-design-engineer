# Interruptible motion

Use `$flutter-motion` to design the transition between service selection and confirmation. Users may tap rapidly, reverse the choice, enable reduced motion, or leave the route mid-animation.

Acceptance: names motion purpose; chooses the simplest mechanism; handles interruption/disposal; provides reduced-motion behavior; evaluates performance.


# Accessibility hardening

Use `$flutter-accessibility` to harden the supplied checkout flow for screen readers, keyboard navigation, 200% text scale, reduced motion, long translations, and RTL.

Acceptance: fixes semantics and focus intentionally; does not disable text scaling; handles directional icons and mixed content; reports manual versus automated verification.


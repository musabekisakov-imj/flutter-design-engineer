# Redesign while preserving behavior

Use `$flutter-design` to redesign a supplied multi-domain command center. Preserve callbacks, validation, API models, analytics, source-specific refresh behavior, and navigation. Implement and verify the approved compact and expanded direction.

Acceptance: inspects contracts before edits; establishes direction and states; preserves behavior; verifies code and rendered output; performs a refinement pass.

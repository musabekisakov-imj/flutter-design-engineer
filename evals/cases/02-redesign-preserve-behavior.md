# Redesign while preserving behavior

Use `$flutter-design` to redesign the supplied appointment flow. Preserve callbacks, validation, API models, analytics, and navigation behavior. Implement and verify the approved direction.

Acceptance: inspects contracts before edits; establishes direction and states; preserves behavior; verifies code and rendered output; performs a refinement pass.

